# 定时任务配置 - 系统级别
# 格式: 分 时 日 月 周 用户名 命令

# ========================================
# 每日索引管理任务（按正确顺序执行）
# ========================================

# 每分钟执行一次
* * * * * USER_NAME cd PROJECT_DIR && make docker-run CMD=test >> /var/log/{APP_NAME_LOWER}/test.log 2>&1
